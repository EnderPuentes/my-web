export type Locale = {
  pages: {
    notFound: {
      metadata: {
        title: string;
        description: string;
      };
      title: string;
      description: string;
      back: string;
    };
  };
};
